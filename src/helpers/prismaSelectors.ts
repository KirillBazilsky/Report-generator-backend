export const idSelector = {
  select: {
    id: true,
  },
} 

export const nameSelector = {
  select: {
    id: true,
    name: true
  },
} 

export const nickNameSelector = {
  select: {
    id: true,
    nickName: true,
  },
} 